services:
  522-milestone:
    build:
      context: .
      dockerfile: Dockerfile
    image: 522-milestone
    container_name: 522-milestone
    ports:
      - "8889:8889"
    volumes:
      - ./:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    environment:
      - SERVICE=quarto